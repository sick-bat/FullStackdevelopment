spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trainingdb
    username: root
    password: alfared
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
       hibernate:
        format_sql: true